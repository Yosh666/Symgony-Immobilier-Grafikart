{% extends "base.html.twig" %}
{% block title %}Modifier un bien{% endblock %}

{% block body %}

    <div class="container mt-4">

        <h1>Editer le Bien </h1>
        <h5>ou le mal au choix....</h5>

        {{ form_start(form)}}
        <div class="row">
            <div class="col-md-4">
                {{ form_row(form.title) }}
            </div>
            <div class="col-md-4">
                {{ form_row(form.surface) }}
            </div>
            <div class="col-md-4">
                {{ form_row(form.price) }}
            </div>
        </div>
        <div class="row">
            <div class="col-md-2">
                {{ form_row(form.room)}}
            </div>
            <div class="col-md-2">
                {{ form_row(form.bedroom)}}
            </div><div class="col-md-3">
                {{ form_row(form.heat)}}
            </div>
            <div class="col-md-2"><!--ASK why ça marche pas
                pensez à virer !es twig comment
                {#{% if {{form_row(form.floor)}}==0 %}
                rez de chaussée
            
                {% else %}
                    {{form_row(form.floor)}}
                {% endif %}#}-->
            </div>
        </div>
        <div class="row">
            <div class="col-md-5">
                {{ form_row(form.address)}}
            </div>
            <div class="col-md-3">
                {{form_row(form.city)}}
            </div>
            <div class="col-md-2">
                {{ form_row(form.postal_code)}}
            </div>
        </div>
            {{form_rest(form)}}
            <button class="btn btn-primary">Editer</button>
        {{ form_end(form)}}
        





    </div>

{% endblock %}